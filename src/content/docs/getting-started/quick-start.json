{
  "frontmatter": {
    "title": "Quick Start",
    "description": "Run your first LoongClaw tasks in minutes",
    "path": "/docs/getting-started/quick-start",
    "order": 2
  },
  "html": "<h1>Quick Start</h1>\n<p>This guide walks you through your first LoongClaw tasks after installation.</p>\n<h2>Prerequisites</h2>\n<p>Before starting, ensure you have:</p>\n<ol>\n<li>LoongClaw installed (see <a href=\"/docs/getting-started\">Getting Started</a>)</li>\n<li>An API key set in environment variables</li>\n<li>Configuration file generated</li>\n</ol>\n<h2>Step 1: Verify Setup</h2>\n<p>Run the diagnostic command to verify your setup:</p>\n<pre><code class=\"language-bash\">loongclaw doctor\n</code></pre>\n<p>Expected output shows all checks passing:</p>\n<pre><code>✓ Configuration file found\n✓ Provider configuration valid\n✓ API key configured\n✓ Memory system ready\n</code></pre>\n<p>If any check fails, run:</p>\n<pre><code class=\"language-bash\">loongclaw doctor --fix\n</code></pre>\n<h2>Step 2: Your First Chat</h2>\n<p>Start an interactive chat session:</p>\n<pre><code class=\"language-bash\">loongclaw chat\n</code></pre>\n<p>You'll see a prompt like:</p>\n<pre><code>loongclaw chat started (config=/home/user/.loongclaw/config.toml)\nsession=default (type /help for commands, /exit to quit)\nyou>\n</code></pre>\n<h3>Try a Simple Question</h3>\n<pre><code>you> What is the capital of France?\nloongclaw> The capital of France is Paris. It's known for the Eiffel Tower...\n</code></pre>\n<h3>Try a Coding Question</h3>\n<pre><code>you> Write a Python function to check if a number is prime\nloongclaw> Here's a Python function to check if a number is prime:\n\ndef is_prime(n):\n    \"\"\"Check if a number is prime.\"\"\"\n    if n &#x3C;= 1:\n        return False\n    if n &#x3C;= 3:\n        return True\n    if n % 2 == 0 or n % 3 == 0:\n        return False\n    i = 5\n    while i * i &#x3C;= n:\n        if n % i == 0 or n % (i + 2) == 0:\n            return False\n        i += 6\n    return True\n</code></pre>\n<h2>Step 3: Use One-Shot Ask</h2>\n<p>For quick questions without entering interactive mode:</p>\n<pre><code class=\"language-bash\">loongclaw ask --message \"Explain the difference between TCP and UDP\"\n</code></pre>\n<p>This is useful for:</p>\n<ul>\n<li>Quick questions in scripts</li>\n<li>CI/CD integrations</li>\n<li>Automated workflows</li>\n</ul>\n<h2>Step 4: Session Management</h2>\n<h3>Create Named Sessions</h3>\n<p>Sessions isolate conversations for different purposes:</p>\n<pre><code class=\"language-bash\"># Work session\nloongclaw chat --session work\n\n# Project-specific session\nloongclaw chat --session my-project\n\n# Learning session\nloongclaw chat --session python-learning\n</code></pre>\n<h3>Session Benefits</h3>\n<ul>\n<li><strong>Isolation</strong>: Each session has independent history</li>\n<li><strong>Persistence</strong>: Conversations saved across restarts</li>\n<li><strong>Organization</strong>: Separate contexts for different tasks</li>\n</ul>\n<h2>Step 5: Using Tools</h2>\n<p>LoongClaw can use built-in tools when enabled. Configure tools in <code>~/.loongclaw/config.toml</code>:</p>\n<pre><code class=\"language-toml\">[tools]\nshell_default_mode = \"deny\"\nshell_allow = [\"ls\", \"cat\", \"echo\", \"git\"]\n</code></pre>\n<p>Then in chat:</p>\n<pre><code>you> List files in the current directory\nloongclaw> I'll list the files for you.\n\n[Invoking shell.exec]\n$ ls -la\ntotal 32\ndrwxr-xr-x  5 user user 4096 Jan 15 10:00 .\ndrwxr-xr-x  2 user user 4096 Jan 15 09:50 src\n...\n\nThe current directory contains:\n- src/ directory\n- README.md file\n</code></pre>\n<h2>Step 6: List Available Models</h2>\n<p>Check which models are available from your provider:</p>\n<pre><code class=\"language-bash\"># Text format\nloongclaw list-models\n\n# JSON format\nloongclaw list-models --json\n</code></pre>\n<h2>Step 7: View Configuration</h2>\n<p>Inspect your current configuration:</p>\n<pre><code class=\"language-bash\"># Validate configuration\nloongclaw validate-config\n\n# View config file\ncat ~/.loongclaw/config.toml\n</code></pre>\n<h2>Common Workflows</h2>\n<h3>Code Review Session</h3>\n<pre><code class=\"language-bash\"># Start a code review session\nloongclaw chat --session code-review\n\nyou> Review this code for potential bugs:\n... [paste code]\n</code></pre>\n<h3>Learning Session</h3>\n<pre><code class=\"language-bash\"># Start a learning session\nloongclaw chat --session rust-learning\n\nyou> Explain Rust's borrowing and ownership model\n</code></pre>\n<h3>Quick Script</h3>\n<pre><code class=\"language-bash\"># One-shot task\nloongclaw ask --message \"Generate a bash script to backup a directory\"\n</code></pre>\n<h2>Keyboard Shortcuts</h2>\n<table>\n<thead>\n<tr>\n<th>Shortcut</th>\n<th>Action</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>Enter</code></td>\n<td>Send message</td>\n</tr>\n<tr>\n<td><code>Shift+Enter</code></td>\n<td>New line (multi-line input)</td>\n</tr>\n<tr>\n<td><code>Ctrl+C</code></td>\n<td>Cancel current input</td>\n</tr>\n<tr>\n<td><code>Ctrl+D</code></td>\n<td>Exit chat</td>\n</tr>\n</tbody>\n</table>\n<h2>Chat Commands Reference</h2>\n<table>\n<thead>\n<tr>\n<th>Command</th>\n<th>Description</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>/help</code></td>\n<td>Show help message</td>\n</tr>\n<tr>\n<td><code>/history</code></td>\n<td>Show conversation history</td>\n</tr>\n<tr>\n<td><code>/exit</code>, <code>/quit</code>, <code>/q</code></td>\n<td>Exit chat session</td>\n</tr>\n</tbody>\n</table>\n<h2>Next Steps</h2>\n<p>Now that you've completed the quick start:</p>\n<ol>\n<li><strong><a href=\"/docs/providers\">Configure Providers</a></strong> - Set up additional LLM providers</li>\n<li><strong><a href=\"/docs/channels\">Configure Channels</a></strong> - Set up Telegram or Feishu integration</li>\n<li><strong><a href=\"/docs/tools\">Enable Tools</a></strong> - Configure shell, file, and browser tools</li>\n<li><strong><a href=\"/docs/memory\">Memory Settings</a></strong> - Adjust session and memory settings</li>\n</ol>\n<h2>Troubleshooting Quick Start</h2>\n<h3>Chat Doesn't Respond</h3>\n<ol>\n<li>Check your API key: <code>echo $OPENAI_API_KEY</code></li>\n<li>Run diagnostics: <code>loongclaw doctor</code></li>\n<li>Check network connectivity</li>\n</ol>\n<h3>Tools Not Working</h3>\n<ol>\n<li>Verify tools are configured in <code>config.toml</code></li>\n<li>Check shell commands are in the allow list</li>\n<li>Run <code>loongclaw validate-config</code> to check configuration</li>\n</ol>\n<h3>Session History Missing</h3>\n<ol>\n<li>Verify SQLite path in configuration</li>\n<li>Check database file exists: <code>ls ~/.loongclaw/memory.sqlite3</code></li>\n<li>Ensure you're using the same session name</li>\n</ol>\n",
  "slug": "quick-start"
}