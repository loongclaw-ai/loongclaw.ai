{
  "frontmatter": {
    "title": "Getting Started",
    "description": "Install, configure, and run LoongClaw",
    "path": "/docs/getting-started",
    "order": 1
  },
  "html": "<h1>Getting Started</h1>\n<p>This guide will help you install, configure, and run LoongClaw in minutes.</p>\n<h2>Prerequisites</h2>\n<ul>\n<li>Rust toolchain (1.75+)</li>\n<li>SQLite (if memory-sqlite feature is enabled)</li>\n</ul>\n<h2>Installation</h2>\n<h3>Install from Source</h3>\n<pre><code class=\"language-bash\"># Clone the repository\ngit clone &#x3C;repository>\ncd loongclaw\n\n# Build and install\n./scripts/install.sh --prefix ~/.local/bin --setup\n\n# Or build manually\ncargo build -p loongclaw-daemon --bin loongclawd --release\n\n# Copy to PATH\ncp target/release/loongclawd ~/.local/bin/\n</code></pre>\n<h3>Verify Installation</h3>\n<pre><code class=\"language-bash\">loongclawd --version\nloongclawd --help\n</code></pre>\n<h2>Initial Configuration</h2>\n<h3>1. Generate Default Configuration</h3>\n<pre><code class=\"language-bash\"># Generate default config\nloongclawd setup\n\n# Force overwrite existing config\nloongclawd setup --force\n</code></pre>\n<p>This creates a configuration file at <code>~/.loongclaw/config.toml</code>.</p>\n<h3>2. Configure API Key</h3>\n<p>Set environment variables (recommended approach):</p>\n<pre><code class=\"language-bash\"># OpenAI\nexport OPENAI_API_KEY=\"sk-...\"\n\n# Or Kimi (Moonshot)\nexport MOONSHOT_API_KEY=\"your-kimi-key\"\n\n# Or DeepSeek\nexport DEEPSEEK_API_KEY=\"your-deepseek-key\"\n</code></pre>\n<h3>3. Verify Configuration</h3>\n<pre><code class=\"language-bash\"># Verify configuration validity\nloongclawd doctor\n\n# Auto-fix issues that can be resolved\nloongclawd doctor --fix\n\n# JSON format output\nloongclawd doctor --json\n</code></pre>\n<h2>Start Chatting</h2>\n<pre><code class=\"language-bash\"># Start interactive CLI chat\nloongclawd chat\n\n# Use specific configuration\nloongclawd chat --config /path/to/config.toml\n\n# Specify session ID\nloongclawd chat --session my-session\n</code></pre>\n<h2>Next Steps</h2>\n<ul>\n<li>Read the <a href=\"/docs/configuration\">Configuration Guide</a> for detailed configuration options</li>\n<li>Check out <a href=\"/docs/commands\">Command Line Tools</a> for all available commands</li>\n<li>Learn about <a href=\"/docs/channels\">Channel Integration</a> to configure Telegram or Feishu</li>\n</ul>\n",
  "slug": "index"
}